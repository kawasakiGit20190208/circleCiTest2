version: 2
jobs:
  build:
    working_directory: /repo
    docker:
      - image: atsnngs/force-com:latest
    steps:
      - checkout
      - run:
          command: rm -rf /src /wsdl &&  mv /repo/src /src && mv /repo/wsdl /wsdl

      - run:
          command: export DEPLOY=$([[ $CIRCLE_BRANCH == 'master' ]] && echo 1 || true); /entrypoint/run.sh > result.txt
      - run: echo $RANDOM >> result.txt
      - run: git config user.email "mkawa0520@gmail.com"
      - run: git config user.name "kawasakiGit20190208"
      - run: git add result.txt
      - run: git commit -m 'add test results'
      - run: git push origin master;
